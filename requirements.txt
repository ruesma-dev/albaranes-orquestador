# requirements.txt — sv7 (orquestador — SOLO desarrollo local)
# sv7 desaparece en la nube (lo sustituye la cadena de colas); este
# manifiesto existe para que el entorno local actual siga reproducible
# durante la transición. NO se contenedoriza.
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
sqlalchemy>=2.0.30
psycopg[binary]>=3.1
